package com.tomogle.iemclient.response;

public enum Status {
  // My version of IEM includes only SUCCESS and FAILED but some of the documentation
  // refers to a status of ERROR so it is also included just in case
  SUCCESS, FAILED, ERROR
}
